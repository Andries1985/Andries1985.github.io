runs-on: ubuntu-latest

steps:
  - uses: actions/checkout@v5
  - uses: ruby/setup-ruby@v1
    with:
      ruby-version: 3.2      # ← change this
      bundler-cache: true
  - run: bundle install
  - run: bundle exec jekyll build